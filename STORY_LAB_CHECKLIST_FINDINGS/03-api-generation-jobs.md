# Story Lab API, Generation, and Jobs (Checks 17-24)

## Reviewed files

- API routes: `api/story-lab/jobs.ts`, `api/story-lab/stories.ts`, `api/story-lab/stories/[storyId]/continue.ts`, `api/story-lab/stream/genesis.ts`, `api/story/stream.ts`, `api/story/generate.ts`, `api/story/continue.ts`
- Shared lib/services: `api/_lib/story-lab/jobs/*`, `api/_lib/story-lab/storyLabEngine.ts`, `api/_lib/http/corsPolicy.ts`, `api/_lib/services/*`, `shared/sensitiveTextRedaction.ts`, `api/_lib/utils/logger.ts`
- Tests: `tests/story-lab-job-routes.test.ts`, `tests/story-lab-job-contracts.test.ts`, `tests/story-lab-job-store-config.test.ts`, `tests/story-lab-real-engine.test.ts`, `tests/story-lab-stream-parse.test.ts`, `tests/story-lab-route-status.test.ts`, `tests/story-service-streaming-security.test.ts`, `tests/cors-policy.test.ts`, `tests/export-sanitizer.test.ts`, `tests/log-redaction.test.ts`
- Planning evidence: `STORY_LAB_REAL_ENGINE_EXEC_PLAN.md`, `STORY_LAB_JOB_ROUTES_EXEC_PLAN.md`, `STORY_LAB_PRIVACY_STREAMING_GATES_EXEC_PLAN.md`

| Check ID | Backend Area | Status | Evidence | Why it matters | What remains |
|---|---|---|---|---|---|
| 17 | Story Lab direct generation endpoints (`/api/story-lab/stories`, `/api/story-lab/stories/:storyId/continue`) | Done | `api/story-lab/stories.ts`, `api/story-lab/stories/[storyId]/continue.ts` parse bodies, validate required fields, and return route-appropriate status codes through `getStoryLabResponseStatus`; `tests/story-lab-route-status.test.ts` confirms content-policy/500/503 mappings and redacted error logs. | Confirms core genesis and continuation execution remains callable even while job-based control-plane is layered on top. | Keep these endpoints stable until UI migration is complete so existing behavior remains backward-compatible. |
| 18 | Story Lab streaming behavior | Partial | `api/story-lab/stream/genesis.ts` and `api/story/stream.ts` still accept query-based parameters for SSE; `tests/story-lab-stream-parse.test.ts` confirms query parser behavior but this path still carries request content in URL fields. | Streaming remains available, but URL-based payload transport is a privacy and observability risk and blocks a full privacy-by-default migration. | Move streaming to POST job creation + job events flow (or at minimum stop leaking full blueprints in query strings). |
| 19 | Job create/read API (`/api/story-lab/jobs`) | Done | `api/story-lab/jobs.ts` exports `handleStoryLabJobsRoute`; `api/_lib/story-lab/jobs/jobRouteHandlers.ts` implements POST/GET/create logic with validation and status transitions; `tests/story-lab-job-routes.test.ts` verifies success, opaque `job_<uuid>` ids, and non-durable mode metadata. | Establishes a typed job ingress with create/read contracts and non-durable scaffolding. | Continue wiring clients/UI to consume this route family as the progress source of record. |
| 20 | Job events and status stream APIs | Done | `jobRouteHandlers` supports `/api/story-lab/jobs/:jobId` and `/api/story-lab/jobs/:jobId/events` via the same function, validates `jobId` as opaque, writes `text/event-stream`, replays snapshot events, and closes response; `tests/story-lab-job-routes.test.ts` asserts queued/running/completed snapshots are replayed and connection ends. | Provides resumable polling/event reads without requiring dedicated per-event function files. | Consider adding persistent event buffering/stream-heartbeat behavior if production async progress requires long polling windows. |
| 21 | Job storage/auth mode behavior | Partial | `api/_lib/story-lab/jobs/storyLabJobStoreConfig.ts` defaults to `non_durable_memory`; `postgres` mode is explicit and gated; `jobRouteHandlers` requires auth owner context for durable mode; tests cover unsupported mode fail-closed and owner-required failures. | Demonstrates safe fail-closed storage behavior, but durable execution is still optional and not the default path. | Add Vercel-aware durable store bootstrapping and migration/runbook once workflow queue and migration tooling are ready. |
| 22 | Engine fallback and provider policy | Done | `api/_lib/story-lab/storyLabEngine.ts` applies production fail-closed (`AI_UNAVAILABLE`) when `XAI_API_KEY` is missing, uses mock mode in non-production with `STORY_LAB_FORCE_MOCK`, and converts provider failures to structured API errors; `tests/story-lab-real-engine.test.ts` and `tests/story-lab-route-status.test.ts` cover both mocked and missing-provider behavior. | Prevents silent fallback to fake responses in production while still keeping developer/testing ergonomics. | Ensure operational runbooks explicitly call out the non-durable/no-queue behavior when falling back under this path. |
| 23 | Privacy gates (CORS/export/log redaction) | Done | `api/_lib/http/corsPolicy.ts` enforces origin deny-closed (`403`) for disallowed origins; `api/_lib/services/exportSanitizer.ts` strips dangerous tags/attrs and escapes metadata; `api/_lib/services/exportService.ts` uses sanitizer helpers; `shared/sensitiveTextRedaction.ts` + `api/_lib/utils/logger.ts` sanitize sensitive fields; tests in `tests/cors-policy.test.ts`, `tests/export-sanitizer.test.ts`, `tests/log-redaction.test.ts` confirm behavior. | Private secrets/path data are blocked at route headers, output artifacts, and logs, reducing leak surface outside auth decisions. | Add audit coverage for remaining legacy loggers that may still accept raw objects before they are fully covered by redaction entry points. |
| 24 | Route budget and deployable API count | Done | `scripts/recovery/check-vercel-function-count.sh` expects/validates one jobs route and currently reports `10/12`; current route list includes `api/story-lab/jobs.ts` plus required story and export functions. | Confirms backend additions stayed within the Vercel function budget and did not accidentally reintroduce route spikes. | Keep the guard as a hard check before merging any further route additions and document any future route consolidations in plan files. |

## Band-level estimate (Checks 17-24)

- **Completion estimate:** **83%** (Done: 6, Partial: 2, Not done: 0). The remaining work is concentrated in durable job execution and non-query streaming hardening; core API route and job scaffolding is present and test-backed, but non-durable limitations remain explicitly unaddressed by production durability requirements. |

## Top 3 backend gaps in this band

1. Legacy Story Lab streaming still accepts blueprint fields in query strings (`api/story-lab/stream/genesis.ts`, `api/story/stream.ts`), so privacy hardening is incomplete until streaming is fully job-id based.
2. Job persistence is non-durable by default (`non_durable_memory`) and not yet backed by a queue/workflow; restart/cold-start loss remains a production behavior gap.
3. Durable postgreSQL job mode is implemented in interface/guarding code but not operationally complete for production workflows (requires storage/auth provisioning + route-level migration).
