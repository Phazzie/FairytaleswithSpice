# Tests, Coverage, and CI Audit (Checks 33-39)

Created: 2026-07-04 07:16 EDT
Last updated: 2026-07-04 07:16 EDT

| Check ID | Verification Area | Status | Evidence | Why it matters | What remains |
|---|---|---|---|---|---|
| 33 | Test command surface inventory | Done | `package.json` defines deterministic root commands: `test` → `test:all`, many individual `test:*` targets, `smoke:*`, `recovery:*`; no command exists for `test:coverage`. `scripts` include `recovery:preflight.sh`, `slice-preflight.sh`, `slice-status.sh`, `finish-check.mjs`, and smoke scripts. | Gives explicit command surface for local verification and shows what automation can actually execute. | Keep this map in sync with any newly added tests by updating/adding npm scripts instead of ad-hoc file-level execution. |
| 34 | Test command coverage map (what `test:all` exercises) | Partial | `package.json` `test:all` chains tsx/mjs commands for Story Lab, route, config, quality, and cloud schema/storage surfaces, but excludes several existing test files. General orphan candidates include `test-story-api.mjs`, `story-service.test.mjs`, `verify-ai-fixes.test.ts`, `verify-api-keys.ts`, and `frontend-streaming.test.ts`. Privacy/security/job-contract suites also omitted from `test:all` include `tests/cors-policy.test.ts`, `tests/export-sanitizer.test.ts`, `tests/log-redaction.test.ts`, `tests/story-lab-stream-parse.test.ts`, `tests/story-service-streaming-security.test.ts`, and `tests/story-lab-job-contracts.test.ts`. | A large undocumented test subset means regressions can slip through because the canonical gate is not executing all checked test logic in-repo. | Add a bounded mapping in docs/scripts for all runnable test files or remove/replace orphaned suites; decide for skipped/jest-style tests before claiming full validation. |
| 35 | Coverage threshold configuration (Angular) | Partial | `story-generator/karma.conf.js` has `coverageReporter` + global checks set to 85% across statements/branches/functions/lines. `story-generator/package.json` uses `ng test`; no custom `--coverage` flag in script. | Threshold exists but is not at the requested 90%; and default test command does not guarantee coverage generation in scripted runs. | Make threshold and invocation explicit (currently 85%); move to enforced 90% only after root/API instrumentation is also enforced. |
| 36 | Root/API coverage instrumentation | Not done | Root package scripts and dependencies do not include `c8`/`nyc`/Jest/Vitest coverage config; `test:all` runs functional checks only (`tsx` + `node`) and does not pass coverage flags. `STORY_LAB_FINAL_MERGE_AUDIT_EXEC_PLAN.md` also states root/API coverage is missing and no artifacts currently exist. | Without instrumentation, the repo cannot produce enforceable coverage evidence for API/runtime TypeScript outside Angular. | Add an explicit root/API coverage gate (for `api/_lib`, deployable `api/story*`, and runtime recovery scripts) and make it part of required CI gates. |
| 37 | Generated coverage evidence + artifact hygiene | Not done | No active `coverage/`, `.nyc_output/`, or `story-generator/coverage/` outputs detected in this checkout; plan notes no current artifacts and stale percentage claims in docs. | Lack of evidence means pass/fail statements are unverifiable and can hide drift. | Define output location and retention expectations for each gate, and require artifacts/check logs as part of check-in evidence. |
| 38 | CI/preflight/smoke command coverage | Partial | `.github/workflows/recovery-ci.yml` runs install + `scripts/recovery/preflight.sh --skip-status` + vercel config checks. `preflight` itself validates toolchain, diff sanity, Vercel function budget, Angular+API type checks, `npm test`, Angular build+verify, and optional status. | Preflight gives strong deterministic checks, but CI does not include coverage or smoke verification by default. | Add explicit coverage step to CI once `test:coverage` exists; keep smoke separate with documented credential/env prerequisites. |
| 39 | Recovery and smoke surface confidence by area | Partial | Smoke commands are present and documented: `smoke:story-lab-ui` (mock + optional live mode), `smoke:grok`, and `smoke:story-lab-live-provider`; the provider-dependent commands short-circuit unless envs are explicitly enabled. | Current smoke has high utility but depends on optional env and external services, so it cannot be considered a universal gate. | Classify these as conditional checks; only claim full pass when credentialed/live checks are executed and captured with evidence. |

## Band-level estimate (Checks 33–39)

- **Completion estimate:** ~43% (7 checks total; 1 Done, 4 Partial, 2 Not done/Conditional-equivalent -> equivalent to ~3/7 hard-progress points at current evidence maturity).

## Top verification gaps

1. No enforced root/API coverage command or tool (`c8`/`nyc`/equivalent) and no root coverage artifacts.
2. Several privacy/security/job-contract suites exist but are not part of `test:all`, so the canonical gate is incomplete.
3. Angular coverage threshold remains at 85%, below the 90% target; current `npm test` path does not force coverage output.
