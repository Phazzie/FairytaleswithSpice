# Story Lab Auth, Storage, and Durability Checklist (Checks 25-32)

Band completion estimate for this slice: **69%**

| Check ID | Durability Area | Status | Evidence | Why it matters | What remains |
|---|---|---|---|---|---|
| 25 | Auth provider fail-closed behavior | Done | `api/_lib/story-lab/auth/authPort.ts:34-42` and `api/_lib/story-lab/auth/configuredAuthPort.ts:16-37`; tests in `tests/story-lab-auth.test.ts:24-58` and `tests/story-lab-configured-auth.test.ts:24-39` | Prevents ambient bearer/API-key-like input from creating accounts and ensures unknown/missing providers do not accidentally authenticate users | None. Provider wiring and UI sign-in flow are outside this slice. |
| 26 | Clerk adapter token verification contract | Done | `api/_lib/story-lab/auth/clerkAuthPort.ts:17-57` and tests in `tests/story-lab-clerk-auth.test.ts:26-116` | Confirms Clerk auth only authenticates via explicit verifier + token/cookie extraction and does not trust raw runtime headers by default | Live Clerk verifier wiring is not yet integrated. |
| 27 | Owner authorization enforcement | Done | `api/_lib/story-lab/auth/authorizeProjectAccess.ts:29-33`; in-memory and Postgres store owner checks in `api/_lib/story-lab/storage/inMemoryStoryProjectStore.ts:116-130`, `api/_lib/story-lab/storage/postgresStoryProjectStore.ts:45-71`, `:155-167`, and `:208-210`; tests in `tests/story-lab-storage-port.test.ts:93-115`, `:124-145`, and `tests/story-lab-account-routes.test.ts:315-334` | User-scoped writes/reads/deletes are required; private story data is not exposed cross-owner | No integration checks against a real PostgreSQL instance for owner scope yet. |
| 28 | Project store durability modes and adapters | Partial | Contract mode flags in `api/_lib/story-lab/storage/storyProjectStore.ts:7-7`, memory adapter `:33-35`, `:42-57`, and Postgres adapter `:101-104`; tests in `tests/story-lab-storage-port.test.ts:69-77` and `:125-134` | Clear distinction exists between `non_durable_memory` and `postgres`, avoiding false durability claims | Durable project path is scaffolded and unproven in production/CI DB runs. |
| 29 | Profile store modes and runtime normalization | Partial | `api/_lib/story-lab/profile/storyLabProfileStore.ts:19-25`, `api/_lib/story-lab/profile/inMemoryStoryLabProfileStore.ts:28-29`, and `api/_lib/story-lab/profile/postgresStoryLabProfileStore.ts:67-74`; tests in `tests/story-lab-profile-store.test.ts:110-119` and `:147-166` | Profile defaults and normalization are present and owner-bound before persistence, preventing bad payloads from becoming durable state | Postgres profile persistence remains adapter-only; no live profile save/load proof against provisioned DB. |
| 30 | Account route security and route-level contract | Partial | `api/_lib/story-lab/account/accountRouteHandlers.ts:86-129`, `:274-320`, `:532-549`; tests in `tests/story-lab-account-routes.test.ts:74-95`, `:114-126`, and `:363-390` | Route-level auth requirement, strict body/path validation, and non-durable sync-state messaging (`cloud_unavailable`) are implemented | End-to-end execution currently blocked by missing runtime dependency in this environment, so full route proof is partial. |
| 31 | Cloud storage config, schema, and readiness scaffold | Partial | `api/_lib/story-lab/storage/storyLabCloudStorageConfig.ts:33-57`, `api/_lib/story-lab/storage/storyLabCloudDatabaseReadiness.ts:60-89`, `api/_lib/story-lab/storage/storyLabCloudSchemaMigration.ts:18-37`, `storyLabCloudSchema.sql:1-9`; tests in `tests/story-lab-cloud-storage-config.test.ts:52-75`, `:105-141`, `tests/story-lab-cloud-db-readiness.test.ts:42-70`, and `tests/story-lab-cloud-schema-migration.test.ts:27-50` | Readiness checks and migration helpers are guarded and explicit; shared config avoids accidental startup crashes when DB is absent | No auto-applied migration, no DB provisioning proof, and missing Neon dependency prevents complete cloud-storage execution here. |
| 32 | Live durable cloud sync and provider-backed auth continuity | Not done | `STORY_LAB_AUTH_PROFILE_CLOUD_LIBRARY_EXEC_PLAN.md:39-47` and `:147-154`; test failures for `@neondatabase/serverless` in `tests/story-lab-account-routes.test.ts` / `tests/story-lab-cloud-storage-config.test.ts` / `tests/story-lab-neon-executor.test.ts` runs | This is the user-visible durability milestone: signed-in accounts with durable read/write/list/delete against live Postgres and stable driver/runtime | Provision and wire Neon (or equivalent driver), run migration, then run full account/route storage test suite against a real DB and signed-in flow. |
