# Angular 22 Dependency Investigation Archive

Created: 2026-07-04

This branch is archived evidence only. It is not an active workstream, not a merge candidate, and not a request to repair the old dependency PR in place.

The useful material preserved here is the six-agent investigation under `PR121_AGENT_FINDINGS/` plus the parent synthesis. The conclusion was that the old dependency branch should not be merged as-is. A future Angular-major-upgrade effort should start fresh from current `main` with its own plan, Node target, TypeScript target, package update, and validation gates.

Do not open this branch as a PR.
